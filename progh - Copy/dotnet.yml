name: .NET CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.0.x'

    - name: Restore dependencies
      run: dotnet restore CybersecurityBot/CybersecurityBot.csproj

    - name: Build
      run: dotnet build CybersecurityBot/CybersecurityBot.csproj --no-restore --configuration Release

    - name: Check for warnings
      run: dotnet build CybersecurityBot/CybersecurityBot.csproj --no-restore /warnaserror